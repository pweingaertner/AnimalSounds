# error message for invalid output

    Code
      animal_sounds(c("dog", "cat"), "miaow")
    Condition
      Error in `animal_sounds()`:
      ! `animal` must be a character vector of length 1.
      i It was a character vector of length 2 instead.

